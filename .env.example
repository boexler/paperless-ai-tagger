# Paperless-ngx API
PAPERLESS_BASE_URL=http://paperless:8000
PAPERLESS_API_TOKEN=your-paperless-api-token

# Agent provider: cursor | codex | openrouter
AGENT_PROVIDER=cursor

# Cursor SDK (required when AGENT_PROVIDER=cursor)
CURSOR_API_KEY=cursor_your_api_key_here
CURSOR_MODEL=composer-2.5
# Model parameters as key:value pairs separated by commas (default: fast:false)
CURSOR_MODEL_PARAMS=fast:false
# Optional: log all available Cursor models and parameters at container startup
# CURSOR_LIST_MODELS_ON_STARTUP=true

# OpenAI Codex CLI (required when AGENT_PROVIDER=codex)
# CODEX_API_KEY=sk-your-openai-api-key
# CODEX_MODEL=gpt-5.4-mini
# CODEX_REASONING_EFFORT=low
# CODEX_MODEL_VERBOSITY=low
# CODEX_APPROVAL_POLICY=never
# CODEX_SANDBOX=workspace-write
# CODEX_NETWORK_ACCESS=true
# CODEX_COMMAND=codex
# CODEX_HOME=/data/codex

# OpenRouter single-shot (required when AGENT_PROVIDER=openrouter)
# OPENROUTER_API_KEY=sk-or-v1-your-key
# OPENROUTER_MODEL=nvidia/nemotron-3-ultra-550b-a55b:free
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Defaults: repo URL as HTTP-Referer, app name as X-Title (override if needed)
# OPENROUTER_HTTP_REFERER=https://github.com/boexler/paperless-ai-tagger
# OPENROUTER_APP_NAME=paperless-ai-tagger
# OPENROUTER_MAX_CONTENT_CHARS=1000000
# OPENROUTER_RETRY_ATTEMPTS=3
# OPENROUTER_RETRY_BACKOFF_SECONDS=5

# Webhook receiver (stage 03 -> port 8083)
WEBHOOK_SECRET=change-me-to-a-long-random-string
WEBHOOK_PORT_03=8083
LOG_LEVEL=INFO

# Optional: prompt template filename (resolved to /app/prompts/<name> in Docker)
# Used by cursor/codex MCP agents (OpenRouter uses prompts/openrouter/ instead)
# PROMPT_TEMPLATE=03-tag-document-tax.md

# Optional: full path override (local development)
# PROMPT_TEMPLATE_PATH=../../prompts/03-tag-document-tax.md

# Optional: path to paperless-ngx-mcp binary (default in Docker image; cursor/codex)
# PAPERLESS_MCP_COMMAND=/usr/local/bin/paperless-ngx-mcp

# Optional: skip documents that were already processed (hours)
DEDUP_TTL_HOURS=24
# Optional: ignore the processed-check (still write to processed_documents.json on success)
# DEDUP_SKIP_CHECK=false

# Optional: max parallel tagging jobs per instance (default: 1 = serial queue)
MAX_CONCURRENT_JOBS=1
